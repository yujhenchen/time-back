## 1. License File

- [x] 1.1 Create `LICENSE` at repo root with standard MIT text and correct year/author
- [x] 1.2 Add `"license": "MIT"` field to `package.json`

## 2. Code Security (blocked.tsx)

- [x] 2.1 Replace the `innerHTML`-based entity decoder in `blocked.tsx` with `DOMParser`
- [x] 2.2 Verify decoded trivia renders correctly (e.g. `&quot;` → `"`, `&#039;` → `'`)

## 3. Privacy Disclosure

- [x] 3.1 Create `PRIVACY.md` at repo root documenting all three external services (Open Trivia DB, DuckDuckGo, Google favicons)
- [x] 3.2 State in `PRIVACY.md` that no personal data is collected, stored, or transmitted
