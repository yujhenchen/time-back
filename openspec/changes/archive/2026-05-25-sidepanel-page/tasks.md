## 1. Manifest Setup

- [x] 1.1 Add `"sidePanel"` to the permissions array in `package.json`

## 2. Implement Side Panel

- [x] 2.1 Create `sidepanel.tsx` with `"use client"` directive, import `styles/globals.css` and `Settings` component
- [x] 2.2 Export a default `SidePanel` component that renders `<Settings />`
- [x] 2.3 Verify the build works
